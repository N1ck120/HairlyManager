CREATE DATABASE hm;
USE hm;

CREATE TABLE usr(
    id_usr INT	NOT NULL	AUTO_INCREMENT,
    nm_usr    VARCHAR(90) NOT NULL,
    email   VARCHAR(90) NOT NULL,
    pass   VARCHAR(256) NOT NULL,

    CONSTRAINT PK_User PRIMARY KEY(id_usr)
);

SELECT * FROM usr;
